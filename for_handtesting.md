# Инструкция по ручному тестированию

## 🚀 Запуск приложения

### 1. Активировать виртуальное окружение

```bash
# macOS/Linux
source venv/bin/activate
```

### 2. Запустить сервер

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открыть в браузере

```
http://localhost:8000/
```

---

## 🧪 Тестовый режим SMS

**По умолчанию включён тестовый режим!**

- **Код верификации:** `0000` (для любого номера)
- **SMS не отправляются** (код выводится в консоль)
- **Не требуется** SMS_API_KEY

---

## 📋 Чеклист тестирования

### Сценарий 1: Новый клиент (первый вход)

- [x] Открыть `http://localhost:8000/`
- [ ] Ввести имя (например, "Иван Иванов")
- [ ] Ввести телефон (например, `+79991234567`)
- [ ] Нажать "Продолжить"
- [ ] Проверить: в консоли появился лог `[SMS STUB] For testing, use code: 0000`
- [ ] Ввести код `0000`
- [ ] Нажать "Подтвердить"
- [ ] **Ожидание:** редирект на страницу карты с QR-кодом

### Сценарий 2: Повторный вход (с сессией)

- [ ] После успешной верификации (Сценарий 1) обновить страницу
- [ ] **Ожидание:** сразу показывается карта (без форм и SMS)
- [ ] Закрыть браузер
- [ ] Открыть заново `http://localhost:8000/`
- [ ] **Ожидание:** редирект на карту (сессия действует 30 дней)

### Сценарий 3: Вход с нового устройства (без сессии)

- [ ] Открыть режим инкогнито (или другой браузер)
- [ ] Открыть `http://localhost:8000/`
- [ ] Ввести существующий телефон (например, `+79991234567`)
- [ ] Нажать "Продолжить"
- [ ] Проверить: в консоли лог `[SMS STUB] For testing, use code: 0000`
- [ ] Ввести код `0000`
- [ ] Нажать "Подтвердить"
- [ ] **Ожидание:** редирект на карту

### Сценарий 4: Незавершённая регистрация

- [ ] Начать регистрацию (ввести телефон)
- [ ] Закрыть браузер до ввода кода
- [ ] Открыть заново `http://localhost:8000/`
- [ ] Ввести тот же телефон
- [ ] Нажать "Продолжить"
- [ ] Ввести код `0000`
- [ ] **Ожидание:** редирект на карту

### Дополнительные проверки

- [ ] Кнопка "Выйти" на странице карты → редирект на `/`
- [ ] После выхода повторный вход требует SMS
- [ ] Админ-панель: `http://localhost:8000/admin`
- [ ] Экспорт CSV: `http://localhost:8000/admin/export`
- [ ] QR-код на странице карты сканируется (проверить телефоном)

---

## 🔧 Настройка .env (опционально)

Для локального тестирования **не требуется** изменение `.env`.

### Для продакшена (позже):

```env
# Database
DATABASE_URL=sqlite:///./loyalty.db

# SMS Service (SMS.ru)
SMS_API_KEY=your_real_api_key_here
SMS_SENDER_NAME=Antoshkin

# Отключить тестовый режим
# В app/services/sms_service.py изменить:
# TEST_MODE = False
```

### SECRET_KEY

Для локальной разработки подойдёт любое значение. Для продакшена:

```bash
# Сгенерировать случайный ключ
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## 📊 Ожидаемое поведение

| Страница | URL | Ожидание |
|----------|-----|----------|
| Регистрация | `/` | Форма ввода имени и телефона |
| Верификация | `/verify?phone=...` | Поле ввода 4-значного кода |
| Карта | `/card/{phone}` | QR-код, имя, телефон |
| Админка | `/admin` | Таблица пользователей |
| Экспорт | `/admin/export` | Скачивание CSV файла |

---

## 🐛 Возможные проблемы

### 401 Unauthorized на `/api/me`

**Нормально!** Означает, что нет активной сессии.

### 404 Not Found на `/favicon.ico`

Не критично. Можно добавить иконку позже.

### Код "0000" не работает

Проверить в консоли:
```
[SMS STUB] For testing, use code: 0000
```

Если лога нет — проверить в `app/services/sms_service.py`:
```python
TEST_MODE = True  # Должно быть True
TEST_CODE = "0000"
```

### Страница не обновляется после верификации

Очистить кэш браузера (Cmd+Shift+R на macOS).

---

## 📝 Логи для отладки

При запуске с `--reload` в консоли видно:

```
INFO:     127.0.0.1:57117 - "GET / HTTP/1.1" 200 OK
INFO:     127.0.0.1:57117 - "GET /api/me HTTP/1.1" 401 Unauthorized
INFO:     [SMS STUB] Code 0000 sent to +79991234567
INFO:     [SMS STUB] For testing, use code: 0000
```

---

## ✅ Критерии успешного тестирования

1. Все 4 сценария работают
2. Редиректы между страницами корректны
3. QR-код отображается
4. Кнопка "Выйти" работает
5. Админ-панель показывает пользователей
6. Экспорт CSV скачивает файл

---

## 📬 Контакты для вопросов

- **Разработчик:** Alex (Python Backend Developer)
