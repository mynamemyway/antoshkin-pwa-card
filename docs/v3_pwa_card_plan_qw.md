## Доработки и патчи

### 7.1. Добавить поиск по телефонам в админке
*Уточнить, реализовано ли пролистывание контактов в админке, если контактов более 50-ти?*

**Ответ:**
- **Пролистывание контактов:** Сейчас реализована пагинация — показываются последние 50 записей. Если контактов >50, нужно добавить пагинацию (страницы 1, 2, 3...) или infinite scroll.
- **Поиск по телефонам:** Не реализован. Нужно добавить поле поиска с фильтрацией.

**Варианты реализации:**

**Вариант A: Простой поиск (frontend)**
```javascript
// Добавить поле поиска в admin.html
<input type="text" id="searchPhone" placeholder="Поиск по телефону">

// Фильтрация таблицы на клиенте
function filterTable(searchTerm) {
    // Фильтровать строки таблицы по совпадению с phone
}
```
- ✅ Быстро, просто
- ❌ Не масштабируется при >1000 записей

**Вариант B: Поиск через API (backend)**
```python
# app/api/routers.py
@router.get("/admin/search")
async def search_users(q: str, db: Session):
    users = db.query(User).filter(User.phone.contains(q)).limit(50).all()
    return users
```
- ✅ Работает с большой базой
- ❌ Требует изменения API

**Рекомендация:** Начать с Варианта A, при росте базы >1000 записей перейти на B.

---

### 7.2. Таймер как работает?
- Таймер жизни проверочного кода из СМС заканчивается и сам начинает заново отсчёт? Или есть сценарий по которому он останавливается раньше времени? Не активная вкладка браузера?
- Таймер должен просто остановиться на 00.00 когда время закончится - теперь так и происходит - ок.
- Кнопка "Отправить повторно" работает корректно - тут всё хорошо.
- Закончилось время 5 минут, ввёл код и он не сработал - всё верно
- Закончилось время 5 минут, сразу ввёл код и он сработал - почему? Предполагаю, что он не умирает для уже зарегистрированного пользователя? Проверить.
- При отправке повторного СМС выскакивает некрасивое всплывающее системное уведомление - убрать, оно избыточно.
*В целом таймер работает хорошо, пока не понимаю баги разовые или есть ошибки в логике* 

**Ответы по пунктам:**

1. **Таймер жизни кода:**
   - Код хранится в БД с полем `sms_code_expires_at` (datetime)
   - Таймер на фронтенде просто показывает оставшееся время
   - **Не перезапускается автоматически** — нужно нажать "Отправить повторно"

2. **Таймер остановился на 00:00:**
   - ✅ Так и задумано — код больше не действителен

3. **Код сработал после истечения времени:**
   - **Баг!** Проверка в роутере:
   ```python
   if user.sms_code_expires_at < datetime.utcnow():
       raise HTTPException(status_code=400, detail="SMS code expired")
   ```
   - Возможно, для уже верифицированного пользователя (`user.is_verified=True`) проверка пропускается — это нужно исправить.

4. **Системное уведомление (alert):**
   - **Баг!** В `verify.html` есть `alert('Новый код отправлен!')`
   - Нужно заменить на красивое UI-сообщение.

**Варианты реализации:**

**Исправление alert:**
```javascript
// Было
alert('Новый код отправлен!');

// Стало - показать красивое сообщение
const messageEl = document.getElementById('resendMessage');
messageEl.textContent = '✅ Новый код отправлен!';
messageEl.classList.remove('hidden');
setTimeout(() => messageEl.classList.add('hidden'), 3000);
```

---

### 7.3. Можно ли исправить подсказки на фронтенде на русский язык?
*Всплывающие подсказки и предупреждения выскакивают на английском языке, например "SMS code expired" или предупреждение о том, что нужно ввести корректный номер телефона. Можно ли их перевести на русский язык?*

**Ответ:** Да, можно и нужно!

**Варианты реализации:**

**Вариант A: Перевод на уровне API**
```python
# app/api/routers.py
@router.post("/api/verify")
async def verify_code(...):
    if user.sms_code_expires_at < datetime.utcnow():
        raise HTTPException(status_code=400, detail="Срок действия кода истёк")
```
- ✅ Все сообщения на русском
- ❌ Нужно менять все строки в API

**Вариант B: Перевод на уровне frontend**
```javascript
// templates/verify.html
const errorMessages = {
    'SMS code expired': 'Срок действия кода истёк',
    'Invalid SMS code': 'Неверный код',
    'No SMS code sent': 'Код не был отправлен'
};

errorText.textContent = errorMessages[error.message] || error.message;
```
- ✅ Быстро, централизованно
- ❌ Нужно поддерживать соответствие сообщений

**Рекомендация:** Вариант B — быстрее и проще поддерживать.

---

### 7.4. Сделать возможность тестировать на телефоне
*Прокинуть через ngrok? Есть другие альтернативы для РФ кроме ngrok?*

**Ответ:** Да, нужно для тестирования PWA на реальном устройстве.

**Варианты реализации:**

**Вариант A: ngrok (международный)**
```bash
ngrok http 8000
# Получаешь https://xxxx-xxxx.ngrok.io
```
- ✅ Просто, быстро
- ❌ Могут быть проблемы с оплатой из РФ
- ❌ Ограничения на бесплатном тарифе

**Вариант B: Cloudflare Tunnel (работает в РФ)**
```bash
# Установить cloudflared
brew install cloudflared

# Запустить туннель
cloudflared tunnel --url http://localhost:8000
```
- ✅ Бесплатно
- ✅ Работает в РФ
- ✅ HTTPS из коробки
- ✅ Нет ограничений по времени

**Вариант C: Локальная сеть (только для тестирования)**
```bash
# Запустить с доступом из локальной сети
uvicorn app.main:app --host 0.0.0.0 --port 8000

# Узнать свой IP
ipconfig getifaddr en0  # macOS
# или
ifconfig | grep inet    # Linux

# На телефоне открыть: http://192.168.x.x:8000
```
- ✅ Не нужен внешний доступ
- ❌ Только в одной Wi-Fi сети
- ❌ Нет HTTPS (PWA не будет работать)

**Рекомендация:** Cloudflare Tunnel — бесплатно, HTTPS, работает в РФ.

---

### 7.5. Дизайн карты

*Дизайн карты сделать в стиле и размере apple wallet:*
- Вверху надписи оставить также как есть (название карты и магазина)
- Ниже вставить изображение скидочной карты по горизонтали (макет есть .tif)
- Ниже под изображением карты вставить наш QR
- В самом низу также оставить надписи как есть сейчас
*Итого: вставить макет скидочной карты над QR, сохранив все функциональные и ознакомительные надписи*

**Ответ:** Нужно изменить вёрстку `card.html`.

**Варианты реализации:**

**Структура макета:**
```
┌─────────────────────────────────┐
│   Антоншкин дворик              │  ← Заголовок
│   Карта лояльности              │
├─────────────────────────────────┤
│                                 │
│   [ИЗОБРАЖЕНИЕ КАРТЫ .tif]      │  ← Макет карты
│   (горизонтальное, ~300x150)    │
│                                 │
├─────────────────────────────────┤
│   [QR-код]                      │  ← QR-код
│   (180x180)                     │
├─────────────────────────────────┤
│   Владелец: Иван Иванов         │  ← Данные
│   Телефон: +7 (999) 123-45-67   │
│   Номер карты: #42              │
└─────────────────────────────────┘
```

**Изменения в card.html:**
```html
<!-- Добавить изображение карты -->
<div class="text-center mb-6">
    <img src="/static/card-mockup.tif" 
         alt="Скидочная карта" 
         class="w-full h-auto rounded-lg shadow-md">
</div>

<!-- QR-код ниже -->
<div class="flex justify-center mb-6">
    <div id="qrcode" class="bg-white p-4 rounded-lg shadow-lg"></div>
</div>
```

**Требования:**
- Конвертировать `.tif` в `.png` или `.svg` (веб-форматы)
- Оптимизировать размер (<100KB)
- Добавить в `static/`

---

### 7.6. Улучшение PWA
- Добавление иконок для iOS (apple-touch-icon) - ⚠️ в коде реализовано? куда добавить?
- Splash screen для нативного вида при запуске
- Push-уведомления (требует HTTPS и сервисов)

**Статус:** Опционально, можно добавить позже.

**Варианты реализации:**

**1. Apple Touch Icon**
```html
<!-- templates/base.html -->
<link rel="apple-touch-icon" href="/static/icon-180.png">
```
- Нужно создать PNG 180x180
- ✅ Быстро

**2. Splash Screen**
```json
// static/manifest.json
{
  "splash_pages": ["/splash.html"]
}
```
- Требует отдельный HTML
- ⏳ Средне

---

### 7.7. Улучшение Безопасности

```
#### Rate Limiting для /api/send-sms (опционально)
* **Файл:** `app/middleware/rate_limit.py`
* **Логика:**
  - Максимум 3 SMS в час на один номер
  - Блокировка по IP или phone
  - **Статус:** Не реализовано (можно добавить позже)

#### HTTPS для продакшена (деплой)
* **Файл:** `nginx.conf` или инструкция для Selectel
* **Логика:**
  - SSL-сертификат Let's Encrypt
  - Редирект HTTP → HTTPS
  - Secure cookie только для HTTPS
  - **Статус:** Задача деплоя (не кода)
```

**Статус:** Рекомендуется для продакшена.

**Варианты реализации:**

**1. Rate Limiting для /api/send-sms**
```python
# app/middleware/rate_limit.py
from slowapi import Limiter
limiter = Limiter(key_func=get_remote_address)

@router.post("/api/send-sms")
@limiter.limit("3/hour")
async def send_sms_code(...):
    ...
```
- Пакет: `slowapi`
- ✅ Защищает от накрутки SMS

**2. Хеширование SMS-кодов**
```python
# app/services/sms_service.py
import bcrypt

# При сохранении
hashed = bcrypt.hashpw(code.encode(), bcrypt.gensalt())
user.sms_code_hash = hashed

# При проверке
bcrypt.checkpw(input_code.encode(), user.sms_code_hash)
```
- ✅ Безопаснее (код не хранится в открытом виде)
- ⏳ Требует изменения модели

**3. Валидация User-Agent**
```python
# Middleware для проверки User-Agent
if "bot" in request.headers.get("user-agent", "").lower():
    raise HTTPException(status_code=403, detail="Bots not allowed")
```
- ✅ Простая защита от ботов

---

## Приоритеты доработок

| Приоритет | Задача | Время |
|-----------|--------|-------|
| 🔴 Высокий | 7.3 Перевод на русский | 1 час |
| 🔴 Высокий | 7.4 Тестирование на телефоне | 30 мин |
| 🟡 Средний | 7.2 Исправление alert | 30 мин |
| 🟡 Средний | 7.5 Дизайн карты | 2 часа |
| 🟢 Низкий | 7.1 Поиск в админке | 2 часа |
| 🟢 Низкий | 7.6 PWA улучшения | 4 часа |
| 🟢 Низкий | 7.7 Безопасность | 3 часа |
