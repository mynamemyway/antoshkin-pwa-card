## 🔍 Анализ гипотезы

**Гипотеза:** Два пользователя одновременно регистрируются → оба запроса находят что пользователя нет → оба создают запись → один проходит верификацию, второй нет.

**Механизм:**
```
Время  Запрос A                          Запрос B
----   --------                          --------
T1     SELECT phone — нет                SELECT phone — нет
T2     INSERT user A                     INSERT user B (IntegrityError → retry?)
T3     send_sms() — блокирует 10 сек     send_sms() — ждёт блокировку БД
T4     SMS отправлена                    SMS отправлена
T5     User A верифицирован              User B не верифицирован (SMS не дошла?)
```


# Найденные проблемы:

🔴 Критичные узкие места

1. Синхронные операции в асинхронных эндпоинтах

Проблема: Все эндпоинты объявлены как async def, но используют синхронные операции:
    - db.query() — синхронный blocking I/O
    - db.commit() — синхронный blocking I/O
    - requests.get() в send_sms() — синхронный blocking I/O

Последствия:

    1 @router.post("/api/send-sms", response_model=SMSResponse)
    2 async def send_sms_code(sms_data: SMSRequest, db: Session = Depends(get_db)):
    3     ...
    4     # БЛОКИРУЮЩАЯ операция - requests.get() блокирует весь event loop
    5     sms_sent = send_sms(user.phone, code)  # ← sync blocking call
    6     ...

Когда один пользователь запрашивает SMS, весь event loop блокируется на время HTTP-запроса к SMS.ru (до 10 секунд по timeout). В это время другие запросы ждут.

Решение: Использовать httpx (асинхронный HTTP-клиент) или запускать send_sms() в thread pool.

---

2. Отсутствие изоляции транзакций при одновременной регистрации

Проблема: Race condition при регистрации двух пользователей с одинаковым телефоном:

    1 @router.post("/api/register", response_model=UserOut)
    2 async def register(user_data: UserCreate, db: Session = Depends(get_db)):
    3     existing_user = get_user_by_phone(db, user_data.phone)  # ← SELECT
    4
    5     if existing_user:
    6         return existing_user
    7
    8     # ← RACE CONDITION: между SELECT и INSERT другой запрос может создать пользователя
    9     new_user = create_user(db, user_data.full_name, user_data.phone)  # ← INSERT
    10     return new_user

Сценарий конфликта:
    1. Запрос A: SELECT — пользователя нет
    2. Запрос B: SELECT — пользователя нет
    3. Запрос A: INSERT — успешно
    4. Запрос B: INSERT — ошибка unique constraint (phone unique)

Решение: Использовать INSERT ... ON CONFLICT DO UPDATE или блокировку строки.

---

3. Отсутствие обработки ошибок при одновременной записи SMS-кода

Проблема: В send_sms_code() запись кода и отправка SMS не атомарны:

    1 code = generate_sms_code()
    2 user.sms_code = code  # ← Запись в БД
    3 user.sms_code_expires_at = datetime.utcnow() + timedelta(minutes=5)
    4 db.commit()  # ← Commit
    5
    6 sms_sent = send_sms(user.phone, code)  # ← SMS может не отправиться
    7
    8 if not sms_sent:
    9     raise HTTPException(status_code=500, detail="Failed to send SMS")
    10 # ← Код уже сохранён в БД, но SMS не отправлено

Если SMS не отправится, код останется в БД, но пользователь его не получит.

---

4. SQLite не оптимален для конкурентной записи

Проблема: SQLite использует файловые блокировки:
    - Чтение: не блокируется (множество читателей)
    - Запись: блокирует всю базу данных (один писатель)

При одновременной регистрации двух пользователей:
    - Запись A блокирует БД
    - Запись B ждёт освобождения блокировки
    - Если A выполняет requests.get() (10 секунд), B ждёт всё это время

Решение: PostgreSQL для production с proper connection pooling.

---

📊 Анализ ситуации с "двойными пользователями"

Ты наблюдаешь такую картину:

    1 User A (неверифицирован) — created_at: 2025-03-08 14:30:00
    2 User B (верифицирован)   — created_at: 2025-03-08 14:30:00

Возможные причины:

    1. Два человека с одинаковым телефоном — маловероятно, но возможно (ошиблись номером)

    2. Race condition при регистрации — два запроса одновременно создали двух пользователей:
    - Запрос 1: SELECT — нет пользователя → INSERT
    - Запрос 2: SELECT — нет пользователя → INSERT (ошибка unique constraint, но клиент мог retry)

    3. Повторная отправка формы — пользователь нажал "Продолжить" дважды:
    - Первый запрос: создал пользователя, отправил SMS
    - Второй запрос: нашёл пользователя, отправил SMS ещё раз (перезаписал код)

    4. Сбой при верификации — SMS дошла только одному из двух пользователей, хотя код был записан для обоих

---

✅ Рекомендации по исправлению

Приоритет 1 (критично):

    1. Заменить `requests` на `httpx` в send_sms() для асинхронности
    2. Добавить обработку race condition в register() через try/except IntegrityError
    3. Добавить логирование каждого этапа регистрации для отладки

Приоритет 2 (желательно):

    4. Перейти на PostgreSQL для production
    5. Добавить retry logic для отправки SMS
    6. Добавить rate limiting для предотвращения спама

# План асинхронного рефакторинга Antoshkin Loyalty Card

## Введение

**Проблема:** В базе данных обнаружены пары пользователей с одинаковым `created_at`, где один верифицирован, второй — нет.

**Причина:** Приложение использует синхронные операции в асинхронных эндпоинтах:
- `db.query()` — синхронный blocking I/O
- `db.commit()` — синхронный blocking I/O  
- `requests.get()` в `send_sms()` — синхронный blocking I/O (до 10 секунд)

**Последствия:**
1. Блокировка event loop при запросах к SMS.ru
2. Race condition при одновременной регистрации
3. Блокировки SQLite при конкурентной записи

**Цель рефакторинга:** Сделать все I/O операции асинхронными, устранить race conditions, обеспечить атомарность отправки SMS.

---

## Этап 1: Слой Базы Данных и Моделей

### 1.1. Обновление моделей для асинхронной работы
*   **Цель:** Подготовить модели к работе с `AsyncSession`
*   **Файл:** `app/models.py`
*   **Логика:** Проверить совместимость моделей с `sqlalchemy.ext.asyncio`. При необходимости добавить явные `relationship` с параметрами `lazy="select"` для отложенной загрузки.

### 1.2. Настройка асинхронного движка БД
*   **Цель:** Создать асинхронное подключение к SQLite
*   **Файл:** `app/database.py`
*   **Логика:**
    1.  Импортировать `create_async_engine`, `AsyncSession`, `async_sessionmaker` из `sqlalchemy.ext.asyncio`
    2.  Создать асинхронный движок с URL `sqlite+aiosqlite:///`
    3.  Создать фабрику сессий `async_sessionmaker`
    4.  Обновить функцию `get_db()` для возврата `AsyncSession`

---

## Этап 2: Слой API (Схемы и Роутеры)

### 2.1. Обновление зависимостей
*   **Цель:** Добавить асинхронные библиотеки
*   **Файл:** `requirements.txt`
*   **Логика:**
    1.  Добавить `httpx` (асинхронный HTTP-клиент)
    2.  Добавить `aiosqlite` (асинхронный драйвер SQLite)
    3.  Убедиться, что `sqlalchemy >= 2.0`

### 2.2. Обновление роутеров для async
*   **Цель:** Обновить эндпоинты для вызова асинхронных функций
*   **Файл:** `app/api/routers.py`
*   **Логика:**
    1.  Обновить зависимости эндпоинтов: заменить `Session` на `AsyncSession`
    2.  Обновить вызовы сервисных функций: добавить `await`
    3.  Для `/api/register`: добавить обработку `IntegrityError` (race condition)
    4.  Для `/api/send-sms` и `/api/login`: переместить отправку SMS ДО коммита (атомарность)

---

## Этап 3: Слой Сервисной Логики

### 3.1. Асинхронные CRUD операции
*   **Цель:** Переписать CRUD функции как асинхронные
*   **Файл:** `app/services/crud.py`
*   **Функции / Сигнатуры:**
    *   `async def get_user_by_phone(db: AsyncSession, phone: str) -> Optional[User]`
    *   `async def create_user(db: AsyncSession, full_name: str, phone: str) -> User`
    *   `async def update_user(db: AsyncSession, user: User, update_data: dict) -> User`
*   **Логика:**
    1.  Заменить `db.query()` на `await db.execute(select(...))`
    2.  Заменить `db.commit()` на `await db.commit()`
    3.  Заменить `db.refresh()` на `await db.refresh()`
    4.  Использовать `result.scalar_one_or_none()` для получения результата

### 3.2. Асинхронная отправка SMS
*   **Цель:** Не блокировать event loop при запросе к SMS.ru
*   **Файл:** `app/services/sms_service.py`
*   **Функция / Сигнатура:** `async def send_sms_async(phone: str, code: str) -> Tuple[bool, str]`
*   **Логика:**
    1.  Заменить `requests.get()` на `async with httpx.AsyncClient()` + `await client.get()`
    2.  Сохранить логику тестового режима (возврат "0000")
    3.  Обработать исключения `httpx.TimeoutException`, `httpx.RequestError`
    4.  Вернуть кортеж `(bool, str)` как в синхронной версии

### 3.3. Обработка race condition при регистрации
*   **Цель:** Устранить дублирование пользователей
*   **Файл:** `app/api/routers.py` (эндпоинт `/api/register`)
*   **Логика:**
    1.  Обернуть вызов `create_user()` в `try/except IntegrityError`
    2.  При `IntegrityError`: выполнить `await db.rollback()`, затем повторный `get_user_by_phone()`
    3.  Вернуть существующего пользователя

### 3.4. Атомарная отправка SMS
*   **Цель:** Не сохранять код если SMS не отправлена
*   **Файл:** `app/api/routers.py` (эндпоинты `/api/send-sms`, `/api/login`)
*   **Логика:**
    1.  Сгенерировать код
    2.  Вызвать `await send_sms_async()` ДО записи в БД
    3.  Если SMS не отправлена: выбросить `HTTPException(500)` без коммита
    4.  Если SMS отправлена: записать код, выполнить `await db.commit()`

---

## Этап 4: Тестирование

### 4.1. Обновление фикстур для async
*   **Цель:** Подготовить тесты к асинхронным операциям
*   **Файл:** `tests/conftest.py`
*   **Логика:**
    1.  Заменить `create_engine()` на `create_async_engine()`
    2.  Заменить `sessionmaker()` на `async_sessionmaker()`
    3.  Обновить фикстуру `db()` как `async def`
    4.  Обновить фикстуру `client()` для работы с async сессией

### 4.2. Тестирование конкурентности
*   **Цель:** Убедиться что race conditions устранены
*   **Файл:** `tests/integration/test_concurrent_registration.py`
*   **Логика:**
    1.  Исправить `test_concurrent_verify_same_user` — должен проходить (хотя бы 1 успех)
    2.  Добавить `test_register_race_condition` — 2 одновременных запроса, оба успешны
    3.  Добавить `test_send_sms_failure_no_code_saved` — при ошибке SMS код не сохраняется
    4.  Запустить все concurrent тесты — 100% pass rate

### 4.3. Регрессионное тестирование
*   **Цель:** Убедиться что существующий функционал не сломан
*   **Файл:** Все существующие тесты
*   **Логика:**
    1.  Запустить `pytest tests/` — все 125 тестов должны пройти
    2.  Проверить время выполнения — не должно увеличиться значительно

---

## Критерии приёмки

- [ ] Все 125 существующих тестов проходят
- [ ] `test_concurrent_verify_same_user` проходит (≥1 успех из 5 запросов)
- [ ] `test_register_race_condition` проходит (оба запроса успешны, нет дубликатов)
- [ ] `test_send_sms_failure_no_code_saved` проходит (код не сохраняется при ошибке)
- [ ] 10 concurrent SMS requests выполняются < 2 секунд (не 10 секунд)
- [ ] В логах нет `IntegrityError` при регистрации

---

## Риски и митигация

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Сложность отладки async | Средняя | Добавить логирование с correlation ID |
| Несовместимость библиотек | Низкая | Использовать `run_in_executor` для синхронных вызовов |
| Увеличение времени выполнения тестов | Низкая | Оптимизировать фикстуры, использовать in-memory БД |

---

## Timeline

| Этап | Задачи | Время |
|------|--------|-------|
| 1 | Модели + асинхронный движок БД | 1 час |
| 2 | Обновление зависимостей + роутеры | 1 час |
| 3 | CRUD + SMS service + race condition + атомарность | 3 часа |
| 4 | Фикстуры + concurrent тесты + регрессия | 2 часа |
| **Итого** | | **7 часов** |

---

## Примечание по SQLite

**Вопрос:** Стоит ли переходить на PostgreSQL?

**Решение:** Нет, для текущего проекта SQLite достаточно.

**Обоснование:**
- 98 пользователей за 3 дня в праздники
- Нет высокой конкурентной нагрузки
- SQLite с `WAL mode` справляется с умеренной конкурентной записью
- Переход на PostgreSQL потребует дополнительных затрат на инфраструктуру

**Рекомендация:** Если в будущем нагрузка вырастет (>1000 одновременных пользователей), тогда рассмотреть PostgreSQL с `asyncpg`.