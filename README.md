# Antoshkin Loyalty Card (PWA)

Система цифровых дисконтных карт для цветочного комбината [rassada1.ru](https://rassada1.ru/).

---

## 📱 О проекте

PWA (Progressive Web App) для автоматизации выдачи скидочных карт в розничных точках. Система позволяет клиентам получать виртуальные карты лояльности через смартфон без установки приложения из AppStore/Google Play.

### Преимущества

- **Для клиентов:**
  - Установка в один клик через браузер
  - Не занимает место в памяти телефона
  - Всегда с собой в смартфоне
  - Мгновенная регистрация через SMS

- **Для бизнеса:**
  - Исключение фейковых номеров
  - Единая база клиентов
  - Экспорт данных для маркетинга
  - Работа без интернета (офлайн-режим)

---

## 🔑 Ключевые функции

* **PWA (Progressive Web App):** Установка на смартфон в один клик без сторов.
* **SMS-верификация:** Защита от регистрации фейковых номеров.
* **QR-генерация:** Динамическое создание кода клиента на стороне браузера (qrcode.js).
* **Сессии:** Автоматический вход при повторном открытии (30 дней).
* **Админ-панель:** Просмотр клиентов, экспорт в CSV, поиск по телефону.
* **Инструкция по установке:** Всплывающая подсказка для iOS и Android.

---

## 🛠 Технологический стек

### Backend
* **Python 3.12** — язык программирования
* **FastAPI 0.115+** — веб-фреймворк
* **SQLAlchemy 2.0+** — ORM для работы с БД
* **SQLite** — база данных
* **Jinja2 3.1+** — шаблонизатор HTML
* **Pydantic 2.10+** — валидация данных

### Frontend
* **Tailwind CSS** — стилизация интерфейса
* **QRCode.js** — генерация QR-кодов
* **HTML5/CSS3** — адаптивная вёрстка

### Инфраструктура
* **Docker** — контейнеризация приложения
* **Nginx** — reverse proxy + SSL
* **Let's Encrypt** — HTTPS-сертификаты

---

## 📊 Сценарии использования (User Flow)

```
Открытие приложения
    ↓
Есть ли активная сессия (cookie)?
    ├── ДА → Проверка в БД
    │         ├── Пользователь есть → Сразу показать карту
    │         └── Пользователя нет → Показать форму регистрации
    │
    └── НЕТ → Показать форму регистрации (имя + телефон)
              ↓
              Ввёл данные → Отправка SMS с кодом
              ↓
              Ввёл код → Верификация → Cookie (30 дней) → Карта
```

### Детальные сценарии

**Сценарий 1: Новый пользователь**
1. Открыл приложение → Форма регистрации
2. Ввёл имя и телефон → SMS с кодом
3. Ввёл код → Карта с QR-кодом

**Сценарий 2: Повторный вход (с сессией)**
1. Открыл приложение → Автоматический вход
2. Сразу видит карту

**Сценарий 3: Выход и повторный вход**
1. Нажал "Выйти" → Сессия удалена
2. Открыл приложение → Форма регистрации
3. Ввёл телефон → SMS с кодом
4. Ввёл код → Карта

**Сценарий 4: Установка PWA**
1. Открыл инструкцию (синий блок)
2. iOS: «Поделиться» → «Добавить на экран домой»
3. Android: «⋮» → «Добавить на гл. экран»
4. Иконка появилась на главном экране

---

## 🗺 Схема работы приложения

```
┌─────────────────────────────────────────────────────────────┐
│                    Открытие приложения                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
            ┌────────────────────────────────┐
            │  Есть активная сессия (cookie)?│
            └────────────────────────────────┘
                            │
                    ┌───────┴───────┐
                    │               │
                   ДА              НЕТ
                    │               │
                    ▼               ▼
        ┌─────────────────────┐   ┌──────────────────┐
        │ Проверка в БД       │   │ Форма регистрации│
        └─────────────────────┘   │ (имя + телефон)  │
                │                 └──────────────────┘
        ┌───────┴───────┐               │
        │               │               ▼
       Есть           Нет       ┌───────────────┐
        │               │       │ Отправка SMS  │
        │               ▼       │ с кодом       │
        │       ┌───────────────┐               │
        │       │ Форма ввода   │               │
        │       │ телефона      │               │
        │       └───────────────┘               │
        │               │                       │
        │               ▼                       │
        │       ┌───────────────┐               │
        │       │ Верификация   │               │
        │       │ ✓ Код верный  │               │
        │       └───────────────┘               │
        │               │                       │
        └───────┬───────┘                       │
                │                               │
                └───────────┬───────────────────┘
                            ▼
        ┌────────────────────────────────────────────────┐
        │              Виртуальная карта с QR            │
        │              (показать кассиру)                │
        │                                                │
        │  ┌─────────────────────────────────────────┐   │
        │  │ [QR-код] 100x100px                      │   │
        │  │                                         │   │
        │  │ Иван Иванов        +7 (999) 123-45-67   │   │
        │  └─────────────────────────────────────────┘   │
        └────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
antoshkin-pwa-card/
├── app/
│   ├── api/
│   │   └── routers.py          # API эндпоинты
│   ├── services/
│   │   ├── crud.py             # CRUD операции
│   │   ├── sms_service.py      # SMS-верификация
│   │   ├── session_service.py  # Управление сессиями
│   │   └── phone_service.py    # Валидация телефонов
│   ├── middleware/
│   │   └── auth.py             # SessionAuthMiddleware
│   ├── __init__.py
│   ├── config.py               # Конфигурация
│   ├── database.py             # Подключение к БД
│   ├── main.py                 # Точка входа
│   ├── models.py               # SQLAlchemy модели
│   └── schemas.py              # Pydantic схемы
├── static/
│   ├── manifest.json           # PWA манифест
│   ├── icon-180.png            # Иконка 180x180 (iOS)
│   ├── icon-192.png            # Иконка 192x192
│   ├── icon-512.png            # Иконка 512x512
│   └── card-mockup.png         # Макет карты
├── templates/
│   ├── base.html               # Базовый шаблон
│   ├── index.html              # Регистрация (имя + телефон)
│   ├── verify.html             # SMS-верификация
│   ├── card.html               # Виртуальная карта
│   ├── admin.html              # Админ-панель
│   └── splash.html             # Splash screen
├── tests/
├── .env                        # Переменные окружения
├── .env.example                # Шаблон переменных
├── requirements.txt            # Зависимости
└── README.md                   # Этот файл
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
# Копирование шаблона
cp .env.example .env

# Редактирование .env (при необходимости)
# SMS_API_KEY=your_api_key  # для продакшена
```

### 3. Запуск приложения

```bash
# Запуск сервера разработки
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Приложение доступно по адресу: `http://localhost:8000`

---

## 🧪 Тестирование

### Режим тестирования

По умолчанию включён тестовый режим SMS:
- **Код верификации:** `0000` (для любого номера)
- **SMS не отправляются** (код выводится в консоль)

Для отключения тестового режима в `app/services/sms_service.py`:
```python
TEST_MODE = False  # Использовать реальные SMS
```

### Чеклист для ручного тестирования

- [ ] Регистрация нового пользователя (имя + телефон)
- [ ] Верификация по SMS (код "0000")
- [ ] Повторный вход (с сессией)
- [ ] Вход после выхода (без сессии)
- [ ] Просмотр карты с QR-кодом
- [ ] Кнопка "Выйти"
- [ ] Инструкция по установке PWA (синий блок)
- [ ] Скрытие инструкции ("× Не показывать снова")
- [ ] Повторный показ инструкции ("Показать инструкцию")
- [ ] Админ-панель `/admin`
- [ ] Поиск по телефону в админке
- [ ] Экспорт CSV `/admin/export`

---

## 📋 План разработки

- [x] **Этап 1: Инфраструктура и БД** — Модели, подключение к SQLite
- [x] **Этап 2: API (схемы и роутеры)** — Эндпоинты для регистрации, SMS, верификации
- [x] **Этап 3: Сервисная логика** — CRUD, SMS, сессии, валидация телефонов
- [x] **Этап 4: Frontend & PWA** — Шаблоны, manifest.json, иконки
- [x] **Этап 5: Админ-панель** — Список пользователей, экспорт CSV, поиск
- [x] **Этап 6: Tестирование** — Ручное тестирование
- [ ] **Этап 7: Деплой** — Docker, Nginx, SSL

---

## 🔐 Безопасность

- **HttpOnly Cookie** — Токен сессии недоступен для JavaScript (защита от XSS)
- **Secure Cookie** — Передача только по HTTPS
- **SameSite=Lax** — Защита от CSRF-атак
- **Срок сессии** — 30 дней, затем требуется повторная авторизация
- **SMS-код** — 4 цифры, истекает через 5 минут

---

## 📞 SMS-шлюз

Для продакшена требуется интеграция с SMS-провайдером:

- **SMS.ru** (рекомендуется)
- **SMPP-провайдеры** (для больших объёмов)

### Настройка

В файле `.env`:
```env
SMS_API_KEY=your_api_key_here
SMS_SENDER_NAME=Antoshkin
```

---

## 📊 Админ-панель

### Доступ

```
GET /admin          # Список пользователей
GET /admin/export   # Экспорт в CSV
```

### Функционал

- Просмотр всех зарегистрированных пользователей
- Статус верификации (верифицирован / ожидает)
- Дата регистрации
- **Поиск по телефону** (фильтрация в реальном времени)
- Экспорт базы для email/SMS-рассылок

---

## 🎯 Критические требования (из ТЗ)

- [x] **PWA с установкой на экран**
- [x] **SMS-верификация (4 цифры)**
- [x] **QR-код на клиенте** (100x100px, статичный)
- [ ] **HTTPS (обязательно для PWA)** ⏳ (деплой)
- [x] **Админка с экспортом CSV**
- [x] **FastAPI + SQLite**
- [x] **Tailwind CSS**
- [x] **Запоминание пользователя (сессии 30 дней)**
- [x] **4 сценария входа**
- [x] **Инструкция по установке PWA** (iOS + Android)

---

## 📝 Лицензия

© 2025 Антошкин. Все права защищены.

---

## 📬 Контакты

- **Заказчик:** Цветочный комбинат [rassada1.ru](https://rassada1.ru/)
- **Разработчик:** [mynamemyway](https://github.com/mynamemyway)
